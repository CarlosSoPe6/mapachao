version: "3.2"
services:
  main:
    build: ./api
    image: mapachapo.azurecr.io/main:${DOCKER_TARGET}
    restart: always
    ports:
      - "5000:5000"
    networks:
      - mapachaonetwork 
    volumes:
      - ${WEBAPP_STORAGE_HOME}/app/media
networks:
  mapachaonetwork:
    driver: bridge
